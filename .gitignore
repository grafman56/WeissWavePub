# ── Proprietary: experience-driven signal suite (not distributed) ────────
weisswave/combined.py

# ── Market data & search results (large / regenerable / private) ─────────
market.duckdb
market.duckdb.wal
finder_results.pkl
strategies.json
bot_strategies.json
scans/
agent-tasks/
signals_cache/
grid_cache/
sweep_results/
stock_data/
stock_data_hourly/

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── Local tooling ─────────────────────────────────────────────────────────
.claude/

# opencode: project-local agents are versioned; runtime scratch is not
.opencode/_prompt.txt
agent-tasks/
.opencode/_task*.txt

# ── Working notes: local-only ─────────────────────────────────────────────
# Session rules and the open-decision list. Neither has ever been tracked.
CLAUDE.md
BACKLOG.md
